@model IEnumerable<GeneralStore.MVC.Models.Product>

@{
    ViewBag.Title = "Index";
}

<h2>List of Products</h2>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InventoryCount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.IsFood)
        </th>
        <th></th>
    </tr>
    @foreach (var product in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(productModel => product.Name)
            </td>
            <td>
                @Html.DisplayFor(productModel => product.InventoryCount)
            </td>
            <td>
                @Html.DisplayFor(productModel => product.Price)
            </td>
            <td></td>
            <td>
                @Html.DisplayFor(prductModel => product.IsFood)
            </td>
            <td></td>
        </tr>
    }
</table>



